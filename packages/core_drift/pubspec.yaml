name: core_drift
description: Adaptador Drift/SQLite del event store de lifeos. SQL crudo sobre la database compuesta de la app (ADR-0006).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  core:
    path: ../core
  drift: ^2.20.0
  uuid: ^4.4.0

dev_dependencies:
  lints: ^4.0.0
  sqlite3: ^2.4.0
  test: ^1.25.0
